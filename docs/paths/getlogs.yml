LogExact:
  get:
    tags: [log]
    summary: Exact log by key.
    parameters:
      - in: query
        name: key
        schema:
          type: string
        description: Exact badger key.
    responses:
      "200":
        content:
          application/json:
            schema:
              type: object
        description: "OK"
      "401":
        description: "Not Authorized"
      "403":
        description: "Forbidden"
      "404":
        description: "Key not found"

PlaceLogRangeQuery:
  post:
    tags: [log]
    summary: Place log range query for execution.
    parameters:
    - in: query
      name: token
      schema:
        type: string
      description: Token for authorization, added automatically to telegram links.
    - in: query
      name: query
      schema:
        type: string
      description: Query for 
