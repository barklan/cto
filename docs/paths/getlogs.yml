LogExact:
  get:
    tags:
      - log
    summary: Exact log by key.
    parameters:
      - in: query
        name: key
        schema:
          type: string
        description: Exact badger key.
    responses:
      "200":
        content:
          application/json:
            schema:
              type: object
        description: "OK"
      "401":
        description: "Not Authorized"
      "403":
        description: "Forbidden"

PlaceLogRangeQuery:
