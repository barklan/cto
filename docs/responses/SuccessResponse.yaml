description: Success response
content:
  application/json:
    schema:
      type: object
      required:
        - success
        - status_code
      properties:
        success:
          type: boolean
          default: true
        status_code:
          type: integer
          format: int32
          default: 200