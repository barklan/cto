{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "run.sh",
            "type": "shell",
            "command": "bash -i run.sh ${input:runsh}",
            "isBackground": true,
        }
    ],
    "inputs": [
        {
            "description": "run.sh command",
            "id": "runsh",
            "type": "pickString",
            "options": [
"up", "up:core", "up:porter", "up:loginput", "up:db", "up:support", "psql", "badger:reset", "frontend", "up:fullstack", "ci:direct", "ci:direct:support", "fluentd:push", "docs:dev", "docs:bundle", "proto", "db:makemigrations", "db:migrate", "db:migrate:remote", "logs", 
            ]
        }
    ]
}
